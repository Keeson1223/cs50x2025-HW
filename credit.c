#include <stdio.h>
#include <cs50.h>
int main(void)
{
    long i = get_long("Please enter your debit card or credit card number. ");
    long number = i;
    int position = 0;
    int sum = 0;

    while (number > 0)
    {
        
    }
}